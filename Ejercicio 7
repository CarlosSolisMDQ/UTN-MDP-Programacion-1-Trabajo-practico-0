#include <stdio.h>
#include <stdlib.h>

int main()
{
    // desarrolle un algoritmo que lea dos numeros y los imprima de forma ascendente.
    
    int a, b;
    printf("Ingrese dos numeros:\n");
    scanf("%d %d", &a, &b);
    if (a > b) {
        printf("%d %d", b, a);
    } else {
        printf("%d %d", a, b);
    }

    return 0;
}
